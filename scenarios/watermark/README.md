# watermark 业务数据

配置入口：**[`config/watermark.yaml`](../../config/watermark.yaml)**（prompt / router / mcp / policy / memory 均在 `config/`）。

本目录仅保留 **`docs/`** — RAG 语料（9 篇 Markdown + manifest）。

已删除的 legacy 文件（勿恢复）：`scenario.yaml`、`policy.yaml`、`router/`、`mcp.yaml`、`prompts/`、`resources.yaml`、`memory/`。
